version: '3.8'

services:
  sysbio-mcp-server:
    build: .
    container_name: mcp_wrapper_core
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
